spring.application.name=fastmarket-test

# Define o contexto do profile
spring.profiles.active=test

# Banco de dados de testes (pode apontar para uma instância separada ou um schema de testes)
spring.datasource.url=jdbc:postgresql://localhost:5432/fastmarket_test
spring.datasource.username=postgres
spring.datasource.password=Tartaruga321@
spring.datasource.driver-class-name=org.postgresql.Driver

# Usado em testes - recria o schema toda vez
spring.jpa.hibernate.ddl-auto=create-drop

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=false

# Evita conflito de porta em paralelo com produção
server.port=0

